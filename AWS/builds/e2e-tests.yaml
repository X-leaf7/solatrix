version: 0.2

env:
  git-credential-helper: yes
        
phases:
  install:
    on-failure: ABORT
    runtime-versions:
      nodejs: 16
  build:
    on-failure: ABORT
    commands:
      - echo "Run some E2E tests here!"